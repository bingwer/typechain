{
  "include": ["src"], // 컴파일할 파일의 위치
  "compilerOptions": {
    "outDir": "build", // 컴파일된 파일의 위치
    "target": "ES6", // 컴파일할 자바스크립트 버전
    "lib": ["ES6", "DOM"], // 타입스크립트가 어디서 동작할지 알려줌
    "strict": true,
    "allowJs": true
  }
}
